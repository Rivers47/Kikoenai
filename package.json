{
  "name": "kikoeru",
  "version": "0.11.0",
  "private": true,
  "description": "A self-hosted media server for DLsite voice works — monorepo combining backend (kikoeru-express) and frontend (kikoeru-quasar).",
  "homepage": "https://github.com/kikoeru-project/kikoeru-express",
  "keywords": [
    "kikoeru"
  ],
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build -w frontend",
    "build": "npm run build:frontend",
    "package:windows": "node scripts/pkg-package.js",
    "package:windows:zip": "node scripts/package.js --windows",
    "start": "npm start -w backend",
    "lint": "npm run lint -w backend && npm run test -w frontend",
    "release:patch": "npm version patch",
    "release:minor": "npm version minor",
    "release:major": "npm version major",
    "version": "node scripts/sync-version.js && git add package.json package-lock.json backend/package.json frontend/package.json",
    "postversion": "git push --follow-tags",
    "version:sync": "node scripts/sync-version.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "concurrently": "^8.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {}
}
